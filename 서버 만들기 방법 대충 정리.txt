@@@ 로컬 환경 구축 및 패키지 설치 @@@

PC에 server 폴더 생성 및 이동(npm init -y 실행: Node.js 프로젝트의 설정 파일인 package.json을 생성)

필수 패키지 설치(Express 및 MongoDB 연결을 위한 라이브러리를 설치 npm install express mongoose dotenv)

@@@ 클라우드 데이터베이스 설정 @@@

MongoDB Atlas 가입 및 클라우드 클러스터 생성

데이터베이스 사용자(User) 계정/비밀번호 생성 및 IP 허용(0.0.0.0/0) 설정

Database Connection String(접속 URI) 복사

@@@ 로컬 서버 코딩 및 테스트 @@@

server.js 파일 작성(Express 서버 설정, MongoDB Atlas 접속 연동, 위치 데이터 API 라우터(POST, GET) 구현)

.gitignore 파일 생성(node_modules 폴더와 DB 접속 비밀번호가 GitHub에 올라가지 않도록 설정.)

로컬 실행 테스트(node server.js 명령어로 DB 연결과 서버 가동이 정상 동작하는지 확인)

@@@ GitHub 저장소 연동 @@@

로컬 폴더에서 Git 명령어 실행(git init $\rightarrow$ git add . $\rightarrow$ git commit -m "Initial commit" $\rightarrow$ git push)

@@@ Render 서버 배포 및 DB 최종 연결 @@@

Render 가입 및 New Web Service 생성

GitHub 저장소 연결

배포 설정 지정(Build Command, Start Command, 환경 변수)

최종 배포 및 릴레이 API 작동 테스트(https://capstonedesign-server.onrender.com)